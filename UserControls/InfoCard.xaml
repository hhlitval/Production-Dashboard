<UserControl x:Class="Production_Analysis.UserControls.InfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Production_Analysis.UserControls"
             mc:Ignorable="d">
    <Border Background="#edf6f9" CornerRadius="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Text="Gesamtproduktion" VerticalAlignment="Center" FontSize="20" FontWeight="Normal" Foreground="#2b2d42" Margin="20 0 0 0"/>
            <TextBlock Grid.Column="1" Grid.Row="0" Text="12156320 t" VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold" Foreground="#2b2d42"/>
            <ProgressBar Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Value="90"/>
        </Grid>        
    </Border>
</UserControl>
